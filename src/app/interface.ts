export interface Detail {
  img: string;
  title: string;
  subtitle: string;
}

export interface Card {
  img: string;
  name: string;
  price: number;
}

export interface faq {
  title: string;
  detail: string;
}
